pipeline {
  agent any
  stages {
    stage('Git') {
      agent any
      steps {
        sh 'echo "Git command"'
      }
    }
    stage('Docker') {
      agent any
      steps {
        sh 'echo "Docker command"'
      }
    }

  }
}